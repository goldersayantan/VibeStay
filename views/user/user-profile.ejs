<% layout("../layouts/boilerplate.ejs") %>
<link rel="stylesheet" href="/stylesheets/user/user-profile.css">
<div class="profile-container">
    <div class="profile-card">
        <div class="profile-left">
             <img src="<%= user.profileImage?.url || 'default-avatar.png' %>" alt="Profile Image" class="profile-img">
        </div>
        <div class="profile-right">
            <h2><%= user.username %></h2>
            <p class="email"><%= user.email %></p>
            <p class="joined">
                Joined on:
                <%= new Date(user.createdAt).toDateString() %>
            </p>
            <p class="listing-link">
                <strong><%= listings.length %></strong> Listings Created
            </p>
            <div class="profile-actions">
                <a href="/profile/edit" class="btn edit-btn">Edit Profile</a>
                <a href="/logout" class="btn logout-btn">Logout</a>
            </div>
        </div>
    </div>

    <div class="my-bookings">
        <h3>My Bookings</h3>
        <% if(myBookings.length === 0)  { %>
            <p>No bookings yet.</p>
        <% }else    { %>
            <table class="booking-table">
                <thead>
                    <tr>
                        <th>Property</th>
                        <th>Check In</th>
                        <th>Check Out</th>
                        <th>Rooms</th>
                        <th>Total Price</th>
                        <th>Status</th>
                    </tr>
                </thead>
                <tbody>
                    <% myBookings.forEach(booking => { %>
                        <tr>
                            <td>
                                <%= booking.listing.title %>
                            </td>
                            <td>
                                <%= new Date(booking.checkIn).toDateString() %>
                            </td>
                            <td>
                                <%= new Date(booking.checkOut).toDateString() %>
                            </td>
                            <td>
                                <% booking.rooms.forEach(room => { %>
                                    <div>
                                        <%= room.quantity %> x
                                        <%= room.roomType.split("-").map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(" ") %>
                                    </div>
                                <% }) %>
                            </td>
                            <td>
                                ₹<%= booking.totalPrice %>
                            </td>
                            <td>
                                <% if(booking.status === "pending") { %>
                                    <span class="status-pending">Pending</span>
                                <% }else if(booking.status == "approved")   { %>
                                    <span class="status-approved">Approved</span>
                                <% }else    { %>
                                    <span class="status-rejected">Rejected</span>
                                <% } %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>

    <div class="booking-requests">
        <h3>Booking Requests</h3>
        <% if(bookingRequests.length === 0) {%>
            <p>No Pending Booking Requests.</p>
        <% }else    { %>
            <table class="booking-table">
                <thead>
                    <tr>
                        <th>Guests</th>
                        <th>Property</th>
                        <th>Check In</th>
                        <th>Check Out</th>
                        <th>Total Price</th>
                        <th>Rooms</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% bookingRequests.forEach(booking => { %>
                        <tr>
                            <td><%= booking.user.username %></td>
                            <td><%= booking.listing.title %></td>
                            <td><%= new Date(booking.checkIn).toDateString() %></td>
                            <td><%= new Date(booking.checkOut).toDateString() %></td>
                            <td>₹<%= booking.totalPrice %></td>
                            <td>
                                <% booking.rooms.forEach(room => { %>
                                    <div>
                                        <%= room.quantity %>
                                        <%= room.roomType.split("-").map(word => word.charAt(0).toUpperCase() + word.slice(1)).join(" ") %>
                                    </div>
                                <% }) %>
                            </td>
                            <td>
                                <form action="/bookings/<%= booking._id %>/approve" method="POST" style="display:inline;">
                                    <button type="submit" class="approve-btn">Approve</button>
                                </form>
                                <form action="/bookings/<%= booking._id %>/reject" method="POST" style="display:inline;">
                                    <button type="submit" class="reject-btn">Reject</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>

    <div class="recent-listings">
        <h3>Your Recent Listings</h3>
        <% if (listings.length === 0) { %>
            <p>No listings created yet.</p>
        <% } else { %>
            <div class="listing-grid">
                <% listings.forEach(listing => { %>
                    <div class="listing-card">
                        <img src="<%= listing.images[0]?.url %>" alt="Listing Image">
                        <h4><%= listing.title %></h4>
                        <p>Starting from: ₹<%= listing.startingPrice %>/night</p>
                        <a href="/listings/<%= listing._id %>">
                            View
                        </a>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>
</div>